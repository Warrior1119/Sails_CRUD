"use strict";
/** @function
  @name find - Find Booking type
  @memberof module:Inventory
  @desc Route - {@linkcode GET:/inventory/:id}
  @returns Find - status 200 with content
  @returns [Server error]{@link Server_error}
*/
module.exports = function find(req, res) {
  const json = { name: "abc" };

  return json;
};
